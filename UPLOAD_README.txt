PROJECT 52 V43 — THE RETURN · FINAL STABLE

FOR THE PROJECT-52-THE-RETURN GITHUB PAGES APP

1. Keep the current tracker available and make a full backup from Data first.
2. Open the PROJECT-52-THE-RETURN repository on GitHub.
3. Upload every file and the assets folder from this package into the repository root.
4. Allow GitHub to replace files with matching names, then commit the upload.
5. Wait for GitHub Pages to deploy and open:
   https://heymann-dev.github.io/PROJECT-52-THE-RETURN/?v=43#home
6. Refresh once while online. If an older design remains, fully close and reopen
   the installed app or perform one hard refresh so the V43 offline shell installs.
7. Open Data > V43 Final Stable System Check and run the complete check.

IMPORTANT DATA RULES

- V43 preserves the familiar military field-command and stone-wall identity while
  using the August 1–December 31, 2026 active campaign.
- Training Command, Appearance / LooksMax, and Lower Cholesterol are first-class,
  persistent systems with mobile routes and full backup/restore support.
- The active phase starts at Level 0 with zero active XP, streaks, time-based
  qualifications, stickers, and progress. Previous evidence remains in the
  read-only PREVIOUS PROJECT 52 ARCHIVE.
- Daily completion requires Critical + Body + Life Missions, the quick four-area
  pain review, and one meaningful Rebuild Entry. A legitimate Recovery version
  counts when it is the correct assignment; the Optional Power Move never does.
- Training clearances and symptoms override the weekly template. Time elapsed
  since surgery never unlocks a movement.
- Uploading these public files does not reset data stored in the same browser app.
- Import the separate V43 private backup only for a fresh installation or a
  deliberate restore. Importing it again can overwrite newer local entries.
- Never upload the private JSON backup or personal photos to a public repository.

The upload package contains public app code and visual assets only. Personal records
remain in the browser unless you deliberately import a private full backup.
